# 🍎 Телеграм-бот подсчета калорий

Умный телеграм-бот, который анализирует фотографии еды с помощью искусственного интеллекта и подсчитывает калории, ведет статистику питания и помогает достигать целей по здоровому питанию.

## ✨ Особенности

- 📸 **Анализ фото еды** - загрузите фото блюда и получите детальную информацию о калориях
- 🤖 **AI-powered анализ** - использует GPT-4 Vision для точного распознавания еды
- 📊 **Статистика питания** - отслеживание калорий, белков, жиров, углеводов
- 🎯 **Персональные цели** - установка и отслеживание дневной нормы калорий
- 📈 **Аналитика** - графики и отчеты по питанию за день, неделю, месяц
- ⚙️ **Настройки профиля** - вес, рост, возраст, пол для точных расчетов
- 🌐 **Готовность к деплою** - поддержка Docker и webhook для продакшена
- 🎨 **Красивый интерфейс** - интуитивные inline-клавиатуры и эмодзи

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- OpenAI API Key (для анализа изображений)

### 1. Клонирование и установка

```bash
git clone <your-repo-url>
cd "AI bot"

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка окружения

Создайте файл `.env` в корне проекта:

```env
# Обязательные настройки
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here

# Настройки базы данных
DATABASE_URL=sqlite:///calorie_bot.db

# Настройки бота
BOT_NAME=Калории Бот 🍎
ADMIN_USER_ID=your_telegram_user_id_here

# Для продакшена (webhook)
WEBHOOK_URL=https://your-server.com
PORT=8000
HOST=0.0.0.0
```

### 3. Запуск

#### Режим разработки (polling):
```bash
python bot.py
```

#### С помощью Docker:
```bash
./start.sh start
```

#### Webhook для продакшена:
```bash
./start.sh webhook
```

## 🔧 Команды управления

Скрипт `start.sh` предоставляет удобные команды:

```bash
./start.sh start     # Запуск бота в режиме polling
./start.sh webhook   # Запуск бота в режиме webhook
./start.sh dev       # Режим разработки без Docker
./start.sh stop      # Остановка всех контейнеров
./start.sh logs      # Просмотр логов
./start.sh build     # Пересборка контейнеров
./start.sh clean     # Полная очистка
./start.sh status    # Статус контейнеров
./start.sh help      # Справка
```

## 📱 Использование бота

### Основные команды

- `/start` - Запуск бота и главное меню
- `/stats` - Показать статистику питания
- `/settings` - Настройки профиля и целей
- `/help` - Справка по использованию

### Анализ еды

1. Сделайте фото вашего блюда
2. Отправьте фото боту
3. Дождитесь анализа (5-10 секунд)
4. Получите информацию о калориях и питательных веществах

### Настройка профиля

- Установите дневную цель калорий
- Укажите вес, рост, возраст
- Выберите уровень активности
- Бот будет отслеживать прогресс к целям

## 🏗 Архитектура проекта

```
AI bot/
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация
├── database.py         # Модели базы данных
├── ai_analyzer.py      # AI анализатор изображений
├── webhook_server.py   # Webhook сервер
├── requirements.txt    # Python зависимости
├── Dockerfile          # Docker контейнер
├── docker-compose.yml  # Docker Compose конфигурация
├── start.sh           # Скрипты запуска
├── init.sql           # Инициализация PostgreSQL
└── README.md          # Документация
```

## 🐳 Docker деплой

### Локальный запуск с SQLite

```bash
docker-compose up -d calorie-bot
```

### Продакшен с PostgreSQL

```bash
docker-compose --profile with-db up -d
```

### Webhook режим

```bash
export WEBHOOK_URL="https://yourdomain.com"
docker-compose --profile webhook --profile with-db up -d
```

## ☁️ Деплой на сервер

### VPS/Dedicated сервер

1. Скопируйте проект на сервер
2. Настройте `.env` файл с production параметрами
3. Запустите в webhook режиме:

```bash
./start.sh webhook
```

### Cloud платформы

Бот готов к деплою на:
- **Railway** - автоматический деплой из Git
- **Heroku** - с PostgreSQL addon
- **DigitalOcean App Platform**
- **AWS ECS** - с RDS базой данных
- **Google Cloud Run**

### Настройка Nginx (reverse proxy)

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

## 📊 База данных

Бот поддерживает две базы данных:

### SQLite (по умолчанию)
- Подходит для разработки и небольших проектов
- Не требует дополнительной настройки
- Файл базы: `calorie_bot.db`

### PostgreSQL (рекомендуется для продакшена)
- Высокая производительность
- Поддержка множественных подключений
- Автоматическая настройка через Docker

### Структура данных

- **users** - пользователи и их настройки
- **food_entries** - записи о приемах пищи
- **daily_stats** - агрегированная дневная статистика

## 🔑 API ключи

### Telegram Bot Token

1. Напишите [@BotFather](https://t.me/BotFather)
2. Создайте нового бота: `/newbot`
3. Выберите имя и username
4. Получите токен и добавьте в `.env`

### OpenAI API Key

1. Зарегистрируйтесь на [OpenAI](https://openai.com)
2. Перейдите в [API Keys](https://platform.openai.com/api-keys)
3. Создайте новый ключ
4. Добавьте в `.env` файл

**Важно:** Убедитесь, что у вас есть доступ к GPT-4 Vision API и достаточный баланс на аккаунте.

## 📈 Мониторинг

### Логирование

Логи записываются в:
- Консоль (при разработке)
- Файлы в папке `logs/` (в Docker)

### Health Check

Webhook сервер предоставляет endpoint для проверки:
- `GET /health` - статус сервера
- `GET /` - базовая проверка

### Метрики

Для мониторинга можно добавить:
- Prometheus метрики
- Grafana dashboard
- Alerting через Telegram

## 🛡 Безопасность

### Переменные окружения

- Никогда не коммитьте `.env` файлы
- Используйте разные ключи для разработки и продакшена
- Регулярно ротируйте API ключи

### База данных

- Используйте сильные пароли
- Настройте файрвол для PostgreSQL
- Делайте регулярные бэкапы

### Webhook

- Используйте HTTPS для webhook URL
- Проверяйте подпись запросов от Telegram
- Ограничивайте rate limit

## 🐛 Отладка

### Частые проблемы

1. **Бот не отвечает**
   - Проверьте токен бота
   - Убедитесь, что бот запущен
   - Проверьте логи: `./start.sh logs`

2. **Ошибка анализа изображений**
   - Проверьте OpenAI API ключ
   - Убедитесь в наличии баланса
   - Проверьте доступ к GPT-4 Vision

3. **Проблемы с базой данных**
   - Проверьте DATABASE_URL
   - Убедитесь, что PostgreSQL запущен
   - Проверьте права доступа

### Включение отладки

В `config.py` измените уровень логирования:

```python
logging.basicConfig(level=logging.DEBUG)
```

## 🤝 Разработка

### Установка для разработки

```bash
pip install -r requirements.txt
pip install pytest black flake8  # dev зависимости
```

### Запуск тестов

```bash
pytest tests/
```

### Форматирование кода

```bash
black .
flake8 .
```

### Структура для новых функций

1. Добавьте обработчик в `bot.py`
2. Обновите модель данных в `database.py`
3. При необходимости расширьте AI анализатор
4. Добавьте тесты
5. Обновите документацию

## 📝 Лицензия

MIT License - можете свободно использовать для коммерческих и некоммерческих проектов.

## 🆘 Поддержка

Если у вас есть вопросы или проблемы:

1. Проверьте раздел [Отладка](#отладка)
2. Посмотрите Issues в GitHub
3. Создайте новый Issue с описанием проблемы

## 🚀 Планы развития

- [ ] Поддержка голосовых сообщений
- [ ] Интеграция с фитнес-трекерами  
- [ ] Рекомендации по питанию
- [ ] Социальные функции (группы, вызовы)
- [ ] Мобильное приложение
- [ ] Поддержка других языков

---

**Создано с ❤️ для здорового питания**
